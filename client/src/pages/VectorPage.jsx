import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const VectorPage = () => {
  return (
    <div>
      <Title level={2}>矢量数据可视化</Title>
      <p>这里是矢量数据的主页面</p>
    </div>
  );
};

export default VectorPage;
