import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const DirectionPage = () => {
  return (
    <div>
      <Title level={3}>方向数据可视化</Title>
      <p>这里是方向数据的详细页面</p>
    </div>
  );
};

export default DirectionPage;
