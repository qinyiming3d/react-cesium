import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const ScalarPage = () => {
  return (
    <div>
      <Title level={2}>标量数据可视化</Title>
      <p>这里是标量数据的主页面</p>
    </div>
  );
};

export default ScalarPage;
