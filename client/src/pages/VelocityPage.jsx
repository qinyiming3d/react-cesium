import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const VelocityPage = () => {
  return (
    <div>
      <Title level={3}>速度数据可视化</Title>
      <p>这里是速度数据的详细页面</p>
    </div>
  );
};

export default VelocityPage;
